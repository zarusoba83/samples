<%
  title = '株主登録';
  currentPage = 'shareholders';
%>
<%- include('../../partials/admin_header') %>

<div class="d-flex align-items-center mb-3">
  <a href="/admin/shareholders" class="btn btn-sm btn-outline-secondary me-2">
    <i class="bi bi-arrow-left"></i>
  </a>
  <h4 class="mb-0"><i class="bi bi-person-plus"></i> 株主登録</h4>
</div>

<div class="card" style="max-width:500px">
  <div class="card-body">
    <% if (error) { %>
      <div class="alert alert-danger"><%= error %></div>
    <% } %>
    <form method="POST" action="/admin/shareholders">
      <div class="mb-3">
        <label class="form-label">株主番号 <span class="text-danger">*</span></label>
        <input type="text" name="shareholder_number" class="form-control"
               value="<%= values.shareholder_number || '' %>" required>
        <div class="form-text">例: SH-001234</div>
      </div>
      <div class="mb-3">
        <label class="form-label">氏名 <span class="text-danger">*</span></label>
        <input type="text" name="name" class="form-control"
               value="<%= values.name || '' %>" required>
      </div>
      <div class="mb-3">
        <label class="form-label">メールアドレス</label>
        <input type="email" name="email" class="form-control"
               value="<%= values.email || '' %>">
      </div>
      <div class="mb-3">
        <label class="form-label">保有株数</label>
        <input type="number" name="shares_count" class="form-control" min="0"
               value="<%= values.shares_count || 0 %>">
      </div>
      <button type="submit" class="btn btn-primary">登録する</button>
    </form>
  </div>
</div>

<%- include('../../partials/admin_footer') %>
